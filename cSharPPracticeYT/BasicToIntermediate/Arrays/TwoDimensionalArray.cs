namespace cSharPPracticeYT.BasicToIntermediate.Arrays;

public class TwoDimensionalArray
{
    public static void Main()
    {
        //int[,] numbers = new int[2,2]; //4>>>>00,01,10,11
        //int[,] numbers2 = { { 1, 2,3 }, { 1, 2,3 } };

        //numbers[0, 0] = 1;
        //numbers[0, 1] = 2;
        //numbers[1, 0] = 3;
        //numbers[1, 1] = 4;

        //int ints = 1;
        //for (int row = 0; row < numbers.GetLength(0); row++)
        //{
        //    for (int col = 0; col < numbers.GetLength(1); col++)
        //    {
        //        numbers[row, col] = ints++;

        //        Console.WriteLine(numbers[row, col]);
        //    }
        //}

        //foreach (var item in numbers)
        //{
        //    Console.WriteLine(item);
        //}


        //int[,] number2d = new int[100,50];

        //int count = 1;

        //for (int row = 0; row < number2d.GetLength(0); row++)
        //{
        //    for(int col = 0; col < number2d.GetLength(1); col++)
        //    {
        //        number2d[row, col] = count++;
        //        Console.Write($"{number2d[row, col]}\t");
        //    }
        //    Console.WriteLine();
        //}

        //char[,] alphabet = new char[3,9];

        //char letter = 'A';

        //for (int row = 0; row < alphabet.GetLength(0); row++)
        //{
        //    for (int col = 0; col < alphabet.GetLength(1); col++)
        //    {
        //        if (letter<='Z')
        //        {
        //            alphabet[row, col] = letter;
        //            letter++;
        //            Console.Write(alphabet[row, col] + " ");
        //        }
        //    }
        //    Console.WriteLine();
        //}
    }
}
